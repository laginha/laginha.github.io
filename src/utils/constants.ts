import { TilePosition, Tiles, PuzzleSize } from './types'

export const EMPTY_TILE_POSITION: TilePosition = 0
export const ORDERED_POSITIONS = [...Array(9).keys()] as Tiles
export const PUZZLE_SIZE: PuzzleSize = 96
